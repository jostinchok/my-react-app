# MySQL connection — adjust if your setup differs
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_DATABASE=park_guide_database
DB_PORT=3306

# Set to false to skip MQTT if you don't need IoT
MQTT_ENABLED=false
DB_PASSWORD=yourpassword
